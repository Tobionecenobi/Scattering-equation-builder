class SubUnit:
    id = "";
    referencepoints = {};
    variable = {};

    # returns the formfactor of the sub unit
    def formfactor(self, parameter_list):
        pass
        return;

    # returns the formfacetor ampltude of the sub unit
    def formfactorAmplitude()
        pass
        return

    # returns the phasefactor of the amplitude 
    def phasefactor()
        pass
        return

    #constructor: Contructs sub unit with an id, its referencepoints its varialbles.
        def __init__(id,referencepoints, variable):
        self.id = id;
        self.referencepoints = referencepoints;
        self.variable = variable; 

    
    
