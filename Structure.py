class Structure:

    #creates an empty table that can store sub units
        
    #creates an empty table that can store information of which subunits is linked

    #creates an empty table that can store information of paths




    #creates sub unit using the subunit class, put that subunit into the subunit table
        def create(subunit()):
            pass
            return;

    #links sub units refenerence point together 
        def link():
            pass
            return;

    #returns the formfactor of the structure build
        def formfactor():
            pass
            return;
    
    #returns the formfactor amplitude of the structure build
        def formfactorAmplitude():
            pass
            return;

    #returns the phasefactor of the structure build
        def phasefactor():
            pass
            return;